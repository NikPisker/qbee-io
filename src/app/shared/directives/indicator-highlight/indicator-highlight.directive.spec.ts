import { IndicatorHighlightDirective } from './indicator-highlight.directive';

describe('IndicatorHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new IndicatorHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
